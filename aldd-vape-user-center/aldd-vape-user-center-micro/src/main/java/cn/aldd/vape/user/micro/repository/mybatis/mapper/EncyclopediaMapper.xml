<?xml version="1.0" encoding="UTF-8"?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.aldd.vape.user.micro.repository.mybatis.dao.EncyclopediaDao">
	
	<select id="findEncyclopediaById" resultType="cn.aldd.vape.user.micro.vo.EncyclopediaVo" parameterType="java.lang.String">
		<include refid="selectAllColumns" />
		where record.id = #{id}
	</select>
	
	<select id="findEncyclopediaList" resultType="cn.aldd.vape.user.micro.vo.EncyclopediaVo" >
		<include refid="selectAllColumns"/>
		<include refid="where_fragement" />
	</select>
	
	<!-- 查询字段公共sql -->
	<sql id="selectAllColumns">
		<![CDATA[
		SELECT
			 id as id, 
			 title as title, 
			 author as author, 
			 content as content, 
			 create_time as createTime 
			FROM encyclopedia record
		]]>
	</sql>

	<sql id="where_fragement">
		where 1=1
		<if test="entity!=null">
				<if test = "entity.id != null and entity.id != ''">
					<![CDATA[ and record.id  = #{entity.id} ]]>
				</if>
				<if test = "entity.title != null and entity.title != ''">
					<![CDATA[ and record.title  = #{entity.title} ]]>
				</if>
				<if test = "entity.author != null and entity.author != ''">
					<![CDATA[ and record.author  = #{entity.author} ]]>
				</if>
				<if test = "entity.content != null and entity.content != ''">
					<![CDATA[ and record.content  = #{entity.content} ]]>
				</if>
				<if test = "entity.createTime != null and entity.createTime != ''">
					<![CDATA[ and record.create_time  = #{entity.createTime} ]]>
				</if>
		</if>
	</sql>

</mapper>